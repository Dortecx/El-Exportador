# Screenshot convention

Store shared documentation screenshots in `docs/images/` using lowercase, hyphenated filenames (for example, `playlist-upload.png`).

When a screenshot is added, `README.md` and `Readme.es.md` must reference the identical image path with localized alt text. Do not include credentials, tokens, cookies, personal data, or local file paths in screenshots.
